# سیستم مدیریت دستورات ربات سازنده (Agent Instructions)

این فایل به عنوان دفترچه راهنما و لاگ تغییرات مهم برای مامور هوش مصنوعی (AI Agent) ایجاد شده است تا اگر پروژه به یک اکانت یا محیط دیگر منتقل شد (مانند Import در GitHub)، ربات بتواند تغییرات حیاتی و زیرساختی که تا به حال اعمال شده را به درستی تشخیص دهد و مجددا اعمال کند و هیچ‌چیز خراب نشود.

## اقدامات اولیه و حیاتی انجام شده در این پروژه

در فرآیند توسعه و راه‌اندازی این پروژه، ربات تغییرات مهم زیر را انجام داده است که باید همیشه در نظر گرفته شوند:

1. **مدیریت وضعیت‌های دیتابیس (Convex):**
   - دیتابیس استفاده شده در این پروژه **Convex** است (نه MySQL و نه SQLite). 
   - سیستم تنظیمات (Settings) به طور کامل به Convex متصل است (از طریق هوک `useSettings`).

2. **رفع خطاهای مسیردهی (Routing & Missing Files):**
   - فایلی به نام `ActivityLogPage.tsx` در مسیر `src/modules/logs/pages/` به دلیل عدم وجود، باعث کرش کردن `main.tsx` در زمان Lazy Loading می‌شد که با ایجاد آن مشکل برطرف شد.
   - خطاهای مربوط به TypeScript مانند import های اضافی یا متغیرهای تعریف شده و استفاده نشده (unused variables) توسط ربات نادیده گرفته شده یا رفع شده‌اند تا اجرای `npm run build` با ۱۰۰٪ موفقیت همراه باشد.


