# نقشه راه توسعه و تکمیل فروشگاه اینترنتی (مشابه استانداردهای دیجی‌کالا)

بر اساس بررسی‌های انجام شده روی امکانات فعلی برنامه (محصولات، سفارشات، کاربران، وبلاگ، تیکت، تخفیف، و...) و مقایسه آن با پلتفرم‌های قدرتمندی مانند دیجی‌کالا، بخش‌های زیر برای تبدیل شدن به یک فروشگاه کامل و حرفه‌ای نیاز است.
این امکانات در ۳ فاز اصلی دسته‌بندی شده‌اند و هیچ‌کدام شامل هوش مصنوعی (AI) نمی‌باشند.

---

## 📦 فاز ۱: تعاملات کاربری، آمار و سئو (اولویت بالا)

این فاز بر روی نگهداشت کاربر، ایجاد اعتماد و جمع‌آوری داده‌های پایه‌ای تمرکز دارد.

### ۱. سیستم آمار بازدید (Site & Product Views)
**توضیحات:** سایت‌های فروشگاهی نیاز به ردیابی دقیق میزان بازدید کل سایت، بازدید صفحات خاص و بازدید هر محصول دارند. این داده‌ها هم برای ادمین (گزارش‌گیری) و هم برای کاربر (نمایش پربازدیدترین‌ها) حیاتی است.
**دیتابیس‌های مورد نیاز (Convex):**
*   **جدول `pageViews` (بازدید صفحات):**
    *   `path`: string (مسیر صفحه مثلا /products/laptop)
    *   `date`: string (تاریخ روز YYYY-MM-DD برای آمار روزانه)
    *   `viewCount`: number (تعداد بازدید)
    *   `uniqueVisitors`: number (تعداد آی‌پی‌های یکتا - هش شده)
*   **جدول `productViews` (یا افزودن فیلد به جدول محصولات):**
    *   `productId`: Id<"products">
    *   `totalViews`: number (مجموع بازدیدها)
    *   `lastViewedAt`: number (timestamp)

### ۲. نظرات پیشرفته و امتیازدهی (Advanced Reviews)
**توضیحات:** در حال حاضر اگر سیستمی برای نظردهی باشد ساده است. سیستم استاندارد باید شامل نقاط قوت، نقاط ضعف، امتیاز (۱ تا ۵) و قابلیت لایک/دیس‌لایک نظرات توسط سایر کاربران باشد. همچنین برچسب "خریدار این محصول" باید نمایش داده شود.
**دیتابیس‌های مورد نیاز:**
*   **جدول `reviews`:**
    *   `productId`: Id<"products">
    *   `userId`: Id<"users">
    *   `rating`: number (۱ تا ۵)
    *   `comment`: string
    *   `pros`: string[] (نقاط قوت)
    *   `cons`: string[] (نقاط ضعف)
    *   `isBuyer`: boolean (آیا خریدار قطعی بوده؟)
    *   `likesCount`: number
    *   `status`: string ("pending", "approved", "rejected")

### ۳. پرسش و پاسخ کاربران (Q&A)
**توضیحات:** بخشی در صفحه محصول که کاربران سوال می‌پرسند و سایر کاربران یا ادمین پاسخ می‌دهند (مشابه دیجی‌کالا).
**دیتابیس‌های مورد نیاز:**
*   **جدول `questions`:**
    *   `productId`: Id<"products">
    *   `userId`: Id<"users">
    *   `content`: string (متن سوال)
    *   `answers`: Array of Objects `{ userId, content, createdAt, isStoreAdmin }`
    *   `status`: string

### ۴. علاقه‌مندی‌ها و اطلاع‌رسانی (Wishlist & Price Alerts)
**توضیحات:** کاربر بتواند محصولات را به لیست علاقه‌مندی اضافه کند و دکمه‌ای به نام «موجود شد خبرم کن» یا «شگفت‌انگیز شد خبرم کن» وجود داشته باشد.
**دیتابیس‌های مورد نیاز:**
*   **جدول `wishlist`:**
    *   `userId`: Id<"users">
    *   `productId`: Id<"products">
    *   `notifyOnStock`: boolean (اطلاع در صورت موجودی)
    *   `notifyOnSale`: boolean (اطلاع در صورت تخفیف)

---

## 🛒 فاز ۲: فروش، بازاریابی و تنوع محصول (اولویت متوسط)

این فاز برای افزایش فروش، مدیریت قیمت‌ها و ترغیب کاربران به خرید مجدد است.

### ۱. متغیرهای محصول پیشرفته (Product Variants)
**توضیحات:** یک محصول (مثلاً گوشی) ممکن است رنگ‌های مختلف (مشکی، سفید) و گارانتی‌های مختلف داشته باشد. هر متغیر باید قیمت و موجودی جداگانه داشته باشد. (در حال حاضر فیلد قیمت روی خود محصول اصلی است).
**دیتابیس‌های مورد نیاز:**
*   **جدول `productVariants`:**
    *   `productId`: Id<"products">
    *   `type`: string (مثلا "color" یا "size" یا "guarantee")
    *   `value`: string (مثلا "مشکی" یا "۱۸ ماهه شرکتی")
    *   `colorCode`: string (اختیاری، مثلا "#000000")
    *   `priceAdjustment`: number (اختلاف قیمت با قیمت پایه، می‌تواند منفی یا مثبت باشد)
    *   `stock`: number (موجودی این تنوع خاص)
    *   `sku`: string (کد انبارداری اختصاصی)

### ۲. کیف پول و کلاب وفاداری (Wallet & Loyalty Club)
**توضیحات:** بازگشت درصدی از مبلغ خرید به صورت "امتیاز" (دیجی‌کلاب) که کاربر بتواند در خریدهای بعدی آن را به کد تخفیف تبدیل کند یا مستقیما از کیف پول پرداخت کند.
**دیتابیس‌های مورد نیاز:**
*   **جدول `wallets`:**
    *   `userId`: Id<"users">
    *   `balance`: number (موجودی ریالی)
    *   `points`: number (امتیازات وفاداری)
*   **جدول `walletTransactions`:**
    *   `walletId`: Id<"wallets">
    *   `amount`: number (مقدار تراکنش مثبت یا منفی)
    *   `type`: string ("deposit", "withdrawal", "purchase", "reward")
    *   `description`: string

### ۳. مدیریت بنرها و لندینگ پیج‌ها (Banners & Campaigns)
**توضیحات:** ادمین باید بتواند بنرهای صفحه اصلی، اسلایدرها و کمپین‌های فروش ویژه (تایمر دار) را از پنل مدیریت بدون نیاز به تغییر کد آپدیت کند.
**دیتابیس‌های مورد نیاز:**
*   **جدول `banners`:**
    *   `title`: string
    *   `imageUrl`: string
    *   `link`: string (لینک مقصد)
    *   `position`: string ("main_slider", "middle_banners", "sidebar")
    *   `isActive`: boolean

---

## 🚚 فاز ۳: خدمات پس از فروش و لجستیک (اولویت توسعه آتی)

این فاز برای فروشگاه‌هایی است که حجم سفارشات بالایی دارند و نیازمند مدیریت دقیق عملیات هستند.

### ۱. سیستم مرجوعی کالا (RMA - Return Merchandise Authorization)
**توضیحات:** کاربر باید بتواند از پنل کاربری خود، برای سفارشاتی که به دستش رسیده (تا ۷ روز) درخواست مرجوعی ثبت کند، عکس خرابی را آپلود کند و ادمین آن را تایید/رد کند.
**دیتابیس‌های مورد نیاز:**
*   **جدول `returnRequests`:**
    *   `orderId`: Id<"orders">
    *   `userId`: Id<"users">
    *   `productId`: Id<"products">
    *   `reason`: string (علت مرجوعی)
    *   `images`: string[] (عکس‌های خرابی)
    *   `status`: string ("pending", "approved", "rejected", "refunded")
    *   `adminNote`: string

### ۲. چند انباره بودن و تامین‌کنندگان (Multi-Vendor / Multi-Warehouse)
**توضیحات:** اگر سایت بخواهد به صورت مارکت‌پلیس (مثل دیجی‌کالا) کار کند که فروشندگان دیگر هم محصول بگذارند، یا خود سایت چند انبار در شهرهای مختلف داشته باشد.
**دیتابیس‌های مورد نیاز (نسخه ساده):**
*   **جدول `vendors` (فروشندگان):**
    *   `userId`: Id<"users">
    *   `storeName`: string
    *   `commissionRate`: number (درصد کمیسیون سایت)
    *   `status`: string ("active", "suspended")
    *(نکته: در این حالت در جدول محصولات یا Variants باید `vendorId` نیز ثبت شود)*
